# Frontend (Vercel)
VITE_API_URL=https://tu-backend.com/api

# Backend (servidor propio o Render/Railway)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password
DB_NAME=taskmanager
JWT_SECRET=tu_jwt_secret_aqui

# Email (Resend)
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=resend
SMTP_PASS=re_tu_api_key
SMTP_FROM="Task Manager <noreply@tudominio.com>"
